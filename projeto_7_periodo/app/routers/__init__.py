"""Routers FastAPI — um módulo por entidade do schema.

Cada router expõe os endpoints REST padrão (``GET``/``POST``/``PATCH``/
``DELETE``) e é registrado em ``app.main`` sob o prefixo definido em
``app.config.API_PREFIX``.
"""
